import numpy as np
from napari_gemspa._gemspa_locate_widget import GEMspaLocateWorker


# def test_update_data(out_dict):
#     # TODO check value of out_dict is as expected given the test data
#     pass
#
#
# def test_locate_worker(qtbot):
#
#     # create our widget, passing in the viewer
#     my_worker = GEMspaLocateWorker()
#
#     # the following raises if the worker's update_data signal (which emits a dict()) wasn't raised with value=?
#     # within the default timeout
#     with qtbot.waitSignal(my_worker.update_data, raising=True, check_params_cb=test_update_data) as blocker:
#         # TODO: need to create input state parameter
#         my_worker.run()






